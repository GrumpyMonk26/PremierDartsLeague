import "./Fixtures.css";

export default function FixturesHero() {
  return (
    <section className="fixtures-hero">
      <div className="fixtures-hero-content">
        <p className="fixtures-subtitle">Premier Darts League</p>

        <h1 className="fixtures-title">Season 4 Fixtures & Results</h1>

        <p className="fixtures-description">
          Select a division and week to view fixtures, completed matches, and
          the latest results from across the league.
        </p>
      </div>
    </section>
  );
}
